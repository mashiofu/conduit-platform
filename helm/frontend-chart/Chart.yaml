apiVersion: v2
name: conduit-frontend
description: Helm chart for the Conduit (RealWorld) Angular frontend, containerized (nginx serving the built SPA)
type: application
version: 0.1.0
appVersion: "1.0.0"
