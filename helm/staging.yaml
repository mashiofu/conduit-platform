# Static, checked-in config for staging. See dev.yaml for the pattern.
awsRegion: us-east-1

monitoring:
  prometheusRetention: 14d
  prometheusRequestsCpu: 250m
  prometheusRequestsMemory: 512Mi
  prometheusStorageSize: 20Gi

backend:
  minReplicas: 2
  maxReplicas: 4
