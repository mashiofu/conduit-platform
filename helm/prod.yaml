# Static, checked-in config for prod. See dev.yaml for the pattern.
awsRegion: us-east-1

monitoring:
  prometheusRetention: 30d
  prometheusRequestsCpu: 500m
  prometheusRequestsMemory: 1Gi
  prometheusStorageSize: 50Gi

backend:
  minReplicas: 3
  maxReplicas: 10
